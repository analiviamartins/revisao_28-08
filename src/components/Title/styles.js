import { StyleSheet } from "react-native-web";

const styles = StyleSheet.create ( {
    title: {
        fontSize: 24,
        marginBotton: 16,
        fontWeight: "bold",
    }
})

export default styles;